{% extends 'base.html.twig' %}

{% block title %}TypesReceipts{% endblock %}

{% block body %}
    <h1>TypesReceipts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ types_receipt.id }}</td>

                
            </tr>
            <tr>
                <th>NametypeRecipe</th>
                <td>{{ types_receipt.nametypeRecipe }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_types_receipts_index') }}">back to list</a>

    <a href="{{ path('app_types_receipts_edit', {'id': types_receipt.id}) }}">edit</a>

    {{ include('types_receipts/_delete_form.html.twig') }}
{% endblock %}
