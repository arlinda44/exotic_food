{% extends 'base.html.twig' %}

{% block title %}Edit Receipts{% endblock %}

{% block body %}
    <h1>Edit Receipts</h1>

    {{ include('receipts/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_receipts_index') }}">back to list</a>

    {{ include('receipts/_delete_form.html.twig') }}
{% endblock %}
